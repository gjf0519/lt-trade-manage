spring:
  application:
    name: lt-trade-business
  cloud:
    nacos:
      discovery:
        server-addr: 212.64.69.77:8848

server:
  port: 7070

management:
  endpoints:
    web:
      exposure:
        include: "*"

feign:
  sentinel:
    enabled: true